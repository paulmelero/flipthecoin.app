// English overrides for the @graficos/nuxt-comments built-in messages.
// They are merged over the package's English defaults, so only translated
// keys are listed here. Applied at runtime when the active locale is `en`.
export const commentsMessagesEn = {
  comments: 'Comments — share your feedback',
};
